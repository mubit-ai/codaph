{
  "name": "@codaph/desktop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "dependencies": {
    "@codaph/adapter-codex-exec": "workspace:*",
    "@codaph/adapter-codex-sdk": "workspace:*",
    "@codaph/core-types": "workspace:*",
    "@codaph/ingest-pipeline": "workspace:*",
    "@codaph/mirror-jsonl": "workspace:*",
    "@codaph/query-service": "workspace:*",
    "electron": "^36.4.0",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.1"
  },
  "scripts": {
    "build": "bun run build:renderer && bun run build:main && bun run build:preload",
    "build:renderer": "vite build -c renderer-vue/vite.config.ts",
    "build:main": "bun build electron-main/src/main.ts --outfile dist/electron-main/main.js --target node --external electron",
    "build:preload": "bun build electron-preload/src/preload.ts --outfile dist/electron-preload/preload.cjs --target node --format cjs --external electron",
    "start": "electron dist/electron-main/main.js",
    "test": "echo \"No desktop tests in this slice\"",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
