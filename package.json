{
  "name": "@codaph/codaph",
  "version": "0.1.18",
  "description": "CLI/TUI for coding-agent activity with shared Mubit memory. Supports Claude Code, Codex, and Gemini CLI.",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "keywords": [
    "codex",
    "claude-code",
    "gemini-cli",
    "cli",
    "tui",
    "mubit",
    "agent",
    "agent-memory",
    "mcp",
    "coding-agent"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mubit-ai/codaph.git"
  },
  "bugs": {
    "url": "https://github.com/mubit-ai/codaph/issues"
  },
  "homepage": "https://github.com/mubit-ai/codaph#readme",
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "codaph": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "docs/one-page-cli-tui.md"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node --packages external && node scripts/prepare-bin.mjs",
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cli": "bun ./src/index.ts",
    "tui": "bun run cli tui",
    "agent:status": "bun run ./src/append-agent-status.ts",
    "hooks:install": "bun run ./src/install-hooks.ts",
    "prepublishOnly": "bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "@mubit-ai/sdk": "^0.5.3",
    "@openai/codex-sdk": "^0.104.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
