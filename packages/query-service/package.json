{
  "name": "@codaph/query-service",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@codaph/core-types": "workspace:*",
    "@codaph/diff-engine": "workspace:*",
    "@codaph/mirror-jsonl": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
